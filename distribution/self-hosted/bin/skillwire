#!/bin/sh
set -eu

release_root="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)"
export SKILLWIRE_RELEASE_ROOT="$release_root"

exec "$release_root/runtime/node" \
  "$release_root/app/skillwire.mjs" \
  "$@"
